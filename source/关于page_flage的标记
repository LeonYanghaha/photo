/*
20170320
每个页面在返回的时候，都有一个参数pag_flage的参数。
关于这个参数的说明如下：
*/
1、这个参数是可以不需要的，但是由于自己前端的水平有限，前端的区块划分不合理。所以需要这个参数加以区分各个页面。
2、这个参数默认是 1 ， 表示使用的共用的页面布局。其包括菜单栏，用户的登录注册信息。

但是对于一些比较特殊的页面，这个参数有可能是以下的情况：
0   首页  有菜单栏，登陆注册，有logo展示
2   关注  有菜单栏，登陆注册，背景图是用户自定义的